/*
 * auxiliary (Aux) components is just a wrapper around multiple components,
 * cause a render function can only return 1 element.
 * 
 */
const auxiliary = props => props.children;

export default auxiliary;